# AI回复内容的markdown语法使用说明

- 当前所有AI回复内容（无论普通文本、代码块、工具调用结果等）都是以 markdown 格式进行渲染。
- 前端通过 MarkdownRenderer 组件将 AI 回复进行 markdown 解析和展示。
- 如果AI回复内容中使用了```代码块```语法（如```html、```js等），页面会自动以高亮、带背景的形式显示；普通文本内容则正常排版。
- 是否以高亮代码形式展示，完全取决于AI回复内容是否采用了markdown的代码块语法。