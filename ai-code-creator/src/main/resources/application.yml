spring:
  application:
    name: ai-code-creator
  # 数据库配置（注意缩进，需在 spring 下）
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai-code-creator?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowMultiQueries=true
    username: root
    password: 123456

server:
  port: 8123
  servlet:
    context-path: /api



knife4j:
  enable: true
  openapi:
    title: ????
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          com/example/code/controller